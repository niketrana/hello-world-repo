name: Checkout Workflow

on: push

jobs:
  checkout-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: List Files
        run: ls -la
