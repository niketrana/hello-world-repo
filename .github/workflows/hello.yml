name: Hello Workflow

on: push

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello from GitHub Actions"

      - name: Show Date
        run: date
