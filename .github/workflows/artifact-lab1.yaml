name: Artifact Lab 1

on: push

jobs:
  artifact-job:
    runs-on: ubuntu-latest

    steps:
      - name: Create File
        run: echo "Hello Artifact Lab" > file.txt

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: my-artifact
          path: file.txt
